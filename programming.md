# 《破晓之种》程序开发执行方案

> 面向第一次接触游戏开发的程序负责人。本文不是概念建议，而是可以逐项执行和验收的开发清单。  
> 技术基线：Godot 4.7.2 Standard、GDScript、Compatibility 渲染器、Windows x86_64、1280×720。  
> 内容基线：关卡流程参考《破晓之种_剧情与玩法策划.md》，结局数量和条件以 `ending.md` 中十二个正式结局为准；旧文档里的“三结局按线索数量划分”不再进入程序实现。  
> 目标：先做出一条可从开场玩到默认结局的完整竖切，再用同一套系统接入另外十一个结局。最终版应让第一次游玩的玩家拥有约 10–20 分钟的有效探索内容，而不是沿单一走廊快速直达终点。

---

## 1. 程序工作能否主要交给 Agent

可以。只要 Godot 工程和素材位于当前共享工作文件夹中，Agent 可以承担绝大多数可落到文件或命令行的工作：

- 创建 Godot 项目、目录、场景、脚本、资源和导出配置。
- 编写玩家移动、芽体轨迹、相机、碰撞、障碍、交互、文本、音频和存档系统。
- 实现十二结局的状态记录、条件判定、演出入口和结局图鉴。
- 生成占位素材和调试界面，使程序不必等待正式美术。
- 读取 Godot 报错、修复脚本、运行无界面检查和自动化测试。
- 整理 Git 提交、`.gitignore`、构建脚本、发布目录和编译说明。
- 在素材到位后批量替换引用、检查缺失文件和重复资源。

你仍然需要亲自负责或最终确认以下事项：

- 第一次在本机打开工程，并在 Godot 编辑器中确认字体、画面、音量和手感是否符合预期。
- 告诉 Agent Godot 可执行文件的绝对路径，或把 Godot 加入系统 `PATH`，否则 Agent 不能直接通过命令行运行工程。
- 登录 Git 托管平台、Game Jam 官网及其他外部账号，处理验证码、权限邀请和最终提交。
- 与队友确认玩法取舍、正式素材版本、著作权和第三方许可证。
- 用真实玩家完成试玩。自动测试能检查逻辑，但无法代替“是否好玩、提示是否看得懂”的判断。
- 在比赛截止前亲自确认最终 ZIP 可以下载、解压和运行。

最适合的合作方式是：Agent 连续完成一个阶段并提交可运行版本，你只需打开游戏试玩 3–10 分钟，反馈“哪里不好玩或看不懂”，Agent 再继续调整。

---

## 2. 你还需要准备什么

### 2.1 必须准备

- **Godot 版本统一**：全队使用完全相同的 Godot 4.7.2 Standard。不要有人使用 Mono/C# 版、有人使用 Standard 版，也不要比赛中途升级引擎。
- **Godot 路径**：已定位并验证 `D:\App\Godot\Godot_v4.7.2-stable_win64.exe`。它暂未加入 Windows `PATH`，但 Agent 可以直接使用绝对路径运行检查；加入 `PATH` 只是方便操作，不再是阻塞项。
- **Git 身份**：执行一次 `git config --global user.name "你的昵称"` 和 `git config --global user.email "你的邮箱"`。邮箱可以使用 GitHub 提供的隐私邮箱。
- **远程仓库**：在 GitHub、Gitee 或 GitLab 建立团队仓库，将其他成员设为协作者。比赛中本机文件不是备份，至少要有一个远程副本。
- **素材交付约定**：让美术和音频按 `need.md` 的英文文件名、格式和透明通道要求交付。程序引用路径一旦确定，后续不要随意改文件名。
- **中文字体及许可证**：至少准备一套可再分发的中文 `.ttf` 或 `.otf`，并保留许可证文件，否则导出后其他电脑可能显示方框。
- **测试电脑**：至少准备另一台没有安装 Godot 的 Windows 电脑，在截止前测试最终导出包。

### 2.2 推荐准备

- **代码编辑器**：Godot 内置编辑器已经够用；也可以安装 VS Code 和 Godot Tools 扩展，但不是必需条件。
- **Git 图形客户端**：小白可使用 GitHub Desktop 查看改动和解决简单冲突；命令行仍由 Agent 处理即可。
- **耳机和普通扬声器**：两种设备都要试听，避免低频冰箱声在普通电脑上完全听不见。
- **手柄**：本届版本不必强制支持。键盘玩法稳定后还有时间再加。
- **Git LFS**：只有在仓库中提交大量 `.psd`、`.kra`、高质量 `.wav` 时才需要。若未安装 LFS，尽量不要把数百 MB 的源文件反复提交。
- **录屏工具**：Windows 截图或 OBS 均可，用于记录 Bug 和制作宣传短视频；不是程序开发的前置条件。

### 2.3 不需要准备

- 不需要购买 Godot 插件或商业框架。
- 不需要数据库、服务器或联网后端。
- 不需要 Blender、复杂骨骼动画工具或物理插件。
- 不需要先学完 GDScript。代码由 Agent 编写，你只要会运行场景、查看报错和描述体验问题。

---

## 3. 技术方案总览

### 3.1 为什么选择 Godot + GDScript

- 项目是小型 2D 游戏，Godot 的场景、碰撞、动画、音频和 UI 已经足够。
- GDScript 语法短，修改后启动速度快，适合 48 小时迭代。
- Standard 版无需配置 .NET SDK，可减少环境问题。
- Compatibility 渲染器对旧电脑和比赛现场设备更稳。
- 所有十二结局复用一张关卡和一套交互系统，不需要十二套独立关卡。

### 3.2 核心结构

```text
输入
  ↓
玩家芽尖移动 ──→ 身体轨迹与局部光照
  ↓
碰撞 / 短按 / 长按 / 绕行 / 静止
  ↓
通用交互对象发送事件
  ↓
GameState 记录本局行为与道具 ──→ GuidanceDirector 选择提示强度
  ↓                                  ├─→ NarrativeManager 播放文字
MapKnowledgeManager 记录已探索区域   ├─→ 角色 / 环境反馈
  ↓                                  └─→ 添加、升级或清除地图标记
小地图 / 完整地图只显示玩家已经获得的知识
  ↓
指定结局入口请求判定
  ↓
EndingResolver 检查条件
  ↓
EndingScreen 播放对应演出并写入 ProgressState
```

设计原则：道具只负责报告“发生了什么”，不自行决定结局；所有结局条件集中在 `ending_resolver.gd` 中。提示系统只能解释玩家已经得到的线索，不能偷偷改动结局条件；地图也只能显示 `MapKnowledgeManager` 已确认的知识，不能直接读取并公开所有随机坐标。

### 3.3 项目目录

比赛最终 ZIP 需要 `source`、`release`、`press`、`other` 四个目录，因此从一开始就按下面组织：

```text
GameJam/
├─ source/                         # 完整 Godot 工程
│  ├─ project.godot
│  ├─ icon.svg
│  ├─ autoload/
│  │  ├─ game_state.gd
│  │  ├─ progress_state.gd
│  │  ├─ audio_manager.gd
│  │  ├─ narrative_manager.gd
│  │  ├─ map_knowledge_manager.gd
│  │  └─ guidance_director.gd
│  ├─ scenes/
│  │  ├─ main/
│  │  │  ├─ main.tscn
│  │  │  └─ main.gd
│  │  ├─ levels/
│  │  │  ├─ fridge_level.tscn
│  │  │  └─ fridge_level.gd
│  │  ├─ player/
│  │  │  ├─ sprout.tscn
│  │  │  ├─ sprout.gd
│  │  │  └─ sprout_visual.gd
│  │  ├─ gameplay/
│  │  │  ├─ pickup.tscn
│  │  │  ├─ pickup.gd
│  │  │  ├─ interactable.tscn
│  │  │  ├─ interactable.gd
│  │  │  ├─ sticky_area.tscn
│  │  │  ├─ frost_area.tscn
│  │  │  ├─ charge_barrier.tscn
│  │  │  ├─ charge_barrier.gd
│  │  │  ├─ ritual_checkpoint.tscn
│  │  │  ├─ ritual_tracker.gd
│  │  │  ├─ narrative_trigger.tscn
│  │  │  └─ ending_entrance.tscn
│  │  └─ ui/
│  │     ├─ main_menu.tscn
│  │     ├─ pause_menu.tscn
│  │     ├─ inventory_panel.tscn
│  │     ├─ inventory_panel.gd
│  │     ├─ minimap_panel.tscn
│  │     ├─ minimap_panel.gd
│  │     ├─ world_map_panel.tscn
│  │     ├─ world_map_panel.gd
│  │     ├─ dialogue_box.tscn
│  │     ├─ ending_screen.tscn
│  │     ├─ ending_screen.gd
│  │     ├─ ending_gallery.tscn
│  │     ├─ ending_gallery.gd
│  │     └─ debug_panel.tscn
│  ├─ systems/
│  │  ├─ ending_resolver.gd
│  │  ├─ ending_ids.gd
│  │  ├─ procedural_map_generator.gd
│  │  ├─ world_chunk_manager.gd
│  │  └─ save_service.gd
│  ├─ data/
│  │  ├─ ending_text.csv
│  │  ├─ narrative_text.csv
│  │  ├─ item_text.csv
│  │  ├─ guidance_rules.csv
│  │  └─ gallery_hints.csv
│  ├─ assets/
│  │  ├─ art/
│  │  ├─ audio/
│  │  ├─ fonts/
│  │  └─ licenses/
│  ├─ tests/
│  │  ├─ ending_resolver_test.gd
│  │  ├─ save_roundtrip_test.gd
│  │  └─ smoke_test.gd
│  └─ docs/
│     └─ debug_guide.md
├─ release/                        # 导出的 exe、pck、README
├─ press/                          # 大于 1024×768 的截图
├─ other/                          # 授权、团队介绍、AI 使用说明
├─ need.md
├─ ending.md
└─ programming.md
```

禁止把 Godot 自动生成的 `.godot/` 目录提交到 Git。`release/` 可以在最终阶段生成，不必每次提交二进制。

---

## 4. Godot 项目初始配置

### 4.1 创建项目

1. 打开 Godot Project Manager。
2. 点击 **Create**。
3. Project Name 填 `Dawn of the Sprout`。
4. Project Path 选择当前目录下的 `GameJam/source`。
5. Renderer 选择 **Compatibility**。
6. Version Control Metadata 选择 **Git**。
7. 创建并打开项目。

如果由 Agent 创建工程，你只需要在 Project Manager 中点击 **Import**，选择 `source/project.godot`。

### 4.2 显示与窗口

在 Project Settings 中设置：

```text
Display > Window > Size > Viewport Width  = 1280
Display > Window > Size > Viewport Height = 720
Display > Window > Stretch > Mode         = canvas_items
Display > Window > Stretch > Aspect       = keep
Rendering > Renderer > Rendering Method   = gl_compatibility
```

开发时允许拖动窗口；正式导出默认窗口模式，不强制全屏。UI 锚点按 1280×720 设计，但必须在 16:9 的常见尺寸下保持可读。

### 4.3 输入动作

在 `Project Settings > Input Map` 创建：

| Action | 默认按键 | 用途 |
| --- | --- | --- |
| `move_up` | W、↑ | 向上生长 |
| `move_down` | S、↓ | 向下生长、反抗结局 |
| `move_left` | A、← | 向左移动 |
| `move_right` | D、→ | 向右移动 |
| `interact` | E | 短按观察/帮助，长按吸收 |
| `charge` | Space | 顶开保鲜膜或盒盖 |
| `restart_run` | R | 结局后重新开始 |
| `pause` | Esc | 暂停菜单 |
| `inventory` | I、Tab | 打开或关闭物品栏/背包 |
| `world_map` | M | 打开或关闭完整地图 |
| `skip_text` | Enter、Space | 二周目快速显示文本 |
| `debug_toggle` | F3 | 仅 Debug 构建显示状态面板 |

同一个 `Space` 在普通游玩中负责蓄力，在文本显示时只负责立即显示全文；输入锁要避免一次按键同时触发两个功能。

### 4.4 Autoload

在 `Project Settings > Globals > Autoload` 注册：

```text
GameState        res://autoload/game_state.gd
ProgressState    res://autoload/progress_state.gd
AudioManager     res://autoload/audio_manager.gd
NarrativeManager res://autoload/narrative_manager.gd
MapKnowledgeManager res://autoload/map_knowledge_manager.gd
GuidanceDirector res://autoload/guidance_director.gd
```

`GameState` 只保存本局数据；`ProgressState` 保存跨局数据。重新开始时只能清空前者。

### 4.5 碰撞层

统一使用固定层，禁止每个人临时乱选：

| Layer | 名称 | 内容 |
| ---: | --- | --- |
| 1 | `world_solid` | 玻璃、包装、墙体、不可穿越冰块 |
| 2 | `player_head` | 玩家芽尖 |
| 3 | `hazard_area` | 黏液、霜毒、腐败区域 |
| 4 | `interactable` | 道具、同伴、机关 |
| 5 | `trigger` | 文本、仪式检查点、结局入口 |
| 6 | `player_body` | 需要检测身体绕行时使用，默认不参与实体碰撞 |

首版只让芽尖发生实体碰撞，Line2D 身体仅负责显示；这能避免长身体卡在复杂地形中的高风险 Bug。

---

## 5. 程序模块设计

### 5.1 `main.tscn`：游戏总入口

职责：

- 启动主菜单。
- 创建或卸载关卡。
- 接收暂停、重新开始和返回菜单请求。
- 在关卡与结局画面之间切换。
- 捕获无法恢复的错误并回到菜单。

不要把玩家移动、道具计数或具体结局判断写入 `main.gd`。

建议节点：

```text
Main (Node)
├─ SceneContainer (Node)
├─ UIContainer (CanvasLayer)
├─ FadeLayer (CanvasLayer)
│  └─ ColorRect
└─ TransitionPlayer (AnimationPlayer)
```

### 5.2 `sprout.tscn`：玩家嫩芽

建议节点：

```text
Sprout (CharacterBody2D)
├─ HeadSprite (Sprite2D)
├─ HeadCollision (CollisionShape2D)
├─ InteractionDetector (Area2D)
│  └─ CollisionShape2D
├─ Trail (Line2D)
├─ PointLight2D
├─ GPUParticles2D
├─ Camera2D
├─ HoldTimer (Timer)
└─ IdleTimer (Timer)
```

移动采用 `CharacterBody2D.move_and_slide()`，不要使用刚体模拟。建议首版参数：

```text
max_speed        = 180 px/s
acceleration     = 900 px/s²
deceleration     = 1200 px/s²
sticky_multiplier = 0.45
frost_multiplier  = 0.65
trail_point_gap   = 8 px
max_trail_points  = 700
```

每帧读取四向输入并归一化，避免斜向速度更快。只有当头部与上一个轨迹点距离超过 `trail_point_gap` 时才向 Line2D 添加点，避免每帧增加一个点造成内存和绘制压力。

必须提供这些公开方法，其他模块不能直接修改玩家内部节点：

```gdscript
set_input_enabled(enabled: bool)
apply_status(status_id: StringName, amount: float)
set_visual_stage(route_id: StringName, stage: int)
add_temporary_speed_modifier(source: StringName, multiplier: float)
remove_speed_modifier(source: StringName)
get_head_position() -> Vector2
get_trail_points() -> PackedVector2Array
```

### 5.3 交互系统

`interactable.gd` 统一处理短按与长按：

- E 按下并在约 0.35 秒内松开：`observe_or_help`。
- E 持续约 0.8 秒：`absorb`。
- 长按进度要显示圆环或进度条；完成前离开范围则取消。
- 一个物体只允许成功一次，成功后关闭碰撞和提示。
- 活物短按增加 `nurture`，长按增加 `devour`；死物按配置执行，不用特殊脚本硬编码。

每个实例只配置数据：

```text
item_id
display_name_myth
display_name_real
interaction_type
short_action
hold_action
state_delta
narrative_text_id
required_item
consumed_after_use
```

### 5.4 障碍系统

- `sticky_area`：进入时添加减速来源，退出时移除；不要直接写死玩家速度。
- `frost_area`：添加霜化状态、短暂减速和视觉结晶；可收集霜晶必须是独立 `pickup`，不能与伤害冰刺使用同一个 ID。
- `charge_barrier`：保鲜膜和盒盖复用。记录蓄力进度，松开时缓慢回退；达到 100% 后播放动画、关闭碰撞并报告破坏行为。
- 玻璃瓶：使用静态碰撞；重撞速度超过阈值才增加 `noise` 和 `bottle_hit_count`，普通擦碰只播放轻声。

建议蓄力值：保鲜膜 0.8–1.2 秒，终局盒盖 1.5–2.0 秒。不要让玩家无反馈地按住超过三秒。

### 5.5 叙事系统

`narrative_trigger` 只发送 `text_id`，`NarrativeManager` 从 `narrative_text.csv` 获取实际文字。它负责：

- 文本排队，不覆盖正在显示的上一条。
- 打字机效果、立即显示全文、自动淡出。
- 同一 `text_id` 默认本局只播放一次。
- 支持条件：首周目、已发现某结局、持有道具、行为阈值。
- 结局演出期间清空普通文本队列并锁定玩家输入。

CSV 建议字段：

```text
text_id,chapter,speaker,zh_cn,duration,once_per_run,condition_tag
```

中文文本可能包含逗号和换行，必须使用正确 CSV 引号；如果队友不熟悉 CSV，可先在 `.md` 中写作，由程序负责人统一转入 CSV。

### 5.6 `GameState`：本局状态

使用明确 API 更新状态，不允许其他脚本随意执行 `GameState.noise += 1`。这样可以统一触发视觉和声音反馈。

建议数据：

```gdscript
var devour: int = 0
var nurture: int = 0
var noise: int = 0
var destruction: int = 0
var corruption: int = 0

var items: Dictionary[StringName, bool] = {}
var counters: Dictionary[StringName, int] = {}
var flags: Dictionary[StringName, bool] = {}
var rituals: Dictionary[StringName, bool] = {}

var last_exit: StringName = &"none"
var run_seconds: float = 0.0
var input_distance: float = 0.0
var optional_nutrients_total: int = 0
var optional_nutrients_eaten: int = 0
```

建议 API：

```gdscript
reset_run()
add_stat(stat_id: StringName, amount: int)
collect_item(item_id: StringName)
has_item(item_id: StringName) -> bool
set_flag(flag_id: StringName, value: bool = true)
has_flag(flag_id: StringName) -> bool
complete_ritual(ritual_id: StringName)
increment_counter(counter_id: StringName, amount: int = 1)
make_snapshot() -> Dictionary
```

所有变化发出信号，例如 `stat_changed`、`item_collected`、`flag_changed`。玩家外观、音频和 UI 监听信号，不轮询状态。

### 5.7 `ProgressState` 与存档

跨局只保存：

```text
save_version
unlocked_ending_ids
run_count
first_ending_seen
text_skip_unlocked
master/music/sfx 音量
```

存档路径使用 Godot 的 `user://save_data.cfg`。不要保存完整关卡坐标和几十个节点状态，48 小时内不做中途存档。

读取失败时：记录警告、备份损坏文件并创建新存档，不能让游戏无法启动。Debug 菜单需要提供“清空图鉴”按钮，但 Release 构建不显示。

### 5.8 音频系统

建立四条 Audio Bus：

```text
Master
├─ Music
├─ Ambience
├─ SFX
└─ UI
```

`AudioManager` 提供 `play_sfx(id)`、`play_music(id, fade_seconds)`、`set_ambience_layer(id, weight)`。所有资源可通过字典预加载，避免场景脚本各自写路径。

克苏鲁路线依赖“第三次压缩机启动”，因此压缩机循环不能只是一段无法知道节拍的长音频。应由 Timer 或 AudioStreamPlayer 的明确重启事件增加 `compressor_start_count`，音频只是表现，程序计时才是判定依据。

### 5.9 仪式与绕圈判定

不要计算玩家是否画了完美圆形。围绕目标放置四个 `Area2D` 检查点：上、左、下、右。

- 逆时针顺序：上 → 左 → 下 → 右 → 上。
- 顺时针顺序：上 → 右 → 下 → 左 → 上。
- 正确经过一个点时播放升调音并点亮刻痕。
- 走错顺序只重置当前进度，不惩罚玩家。
- 离开目标过远或超过 8 秒未到下一点则重置。

同一套 `ritual_tracker.gd` 通过 Inspector 配置顺序，用于辣酱瓶、黄色瓶盖、自身成环和根系连接。

所有仪式实体必须预留检查点净空。检查点中心与实体碰撞边缘的距离不得小于“玩家碰撞半径 + 32 px”，程序化生成器也不得在检查点与路径之间放置墙体、区域或拾取物；该规则需要自动测试覆盖。

### 5.10 物品栏 / 背包

按 `I` 或 `Tab` 打开 `inventory_panel.tscn`，用于查看本局已经获得的道具和数量。背包只是 `GameState.items` 与 `GameState.counters` 的只读视图，不能复制一份独立库存，以免重开或拾取时状态不同步。

- 按遗物、养分/牛奶、线索、组件四类显示；同类按固定 `item_id` 排序，不能因为字典遍历顺序而抖动。
- 每格显示图标、神话名称、数量和简短描述；完成首周目后可额外显示现实名称。文本统一从 `item_text.csv` 读取。
- 未发现道具不在本局背包中显示；结局图鉴的谜语仍由图鉴负责，背包不直接泄露未取得配方。
- 打开背包时暂停玩家移动、计时和交互，但 UI 使用 `PROCESS_MODE_ALWAYS`；再次按 `I`/`Tab` 或 `Esc` 关闭。背包打开时，`Esc` 优先关闭背包而不是同时打开暂停菜单。
- 支持鼠标、键盘焦点导航和 1280×720；道具多于一页时使用滚动容器，不缩小文字。
- 重新开始后，本局背包随 `GameState.reset_run()` 清空；跨局只保留结局图鉴，不保留上一局物品。

### 5.11 `ItemEffectDirector`：道具机制、被动能力与状态效果

当前版本的道具已经能参与结局条件，但“拿到道具之后玩家能做什么”仍然不够明显。最终版需要把道具分成四种作用：改变移动、改变环境交互、改变地图知识、改变叙事反馈。道具效果必须服务于探索和多结局，不把项目变成需要大量计算面板的传统 RPG。

#### 5.11.1 总体规则

- 道具拾取后自动登记到 `GameState.items`，效果由 `ItemEffectDirector` 统一激活；场景、区域和结局判定不能各自硬编码同一道具的效果。
- 效果分为 `passive`（持有即生效）、`contextual`（进入指定区域时生效）、`active`（按键或交互消耗）和 `knowledge`（更新地图/提示）四类。首版优先实现 `passive + contextual + knowledge`，减少新的按键负担。
- 正面效果必须改变一个可感知的玩家行为，例如移动速度、危险抗性、可见范围、捷径或交互时间；不要只增加无法观察的隐藏数值。
- 每个负面效果必须在背包、状态栏或拾取文本中说明来源和解除方式。不可因为玩家不知道规则而永久锁死某个结局。
- 被动效果不删除道具、不减少已有计数，也不能偷偷修改 `ending_resolver.gd` 的结局条件。需要互斥的路线必须在不可逆交互前显示明确警告。
- 环境抗性与结局资源分离：`frost_immunity` 可以免疫 `frost_area` 的减速，但不能阻止收集霜晶或增加 `frost_count`；`attention`、`sprint_noise` 等新效果也不能直接代替 `corruption`、`noise`、`destruction`，除非结局规格明确写出转换规则。
- 同类倍率采用“最高抗性优先、效果不超过 100%”的规则。速度、减速、光照等倍率统一由玩家脚本查询，区域只添加来源 ID，不直接改写 `max_speed`。
- 本局重开时清空所有 `active_effects`；跨局只保留结局图鉴和设置，不保存临时被动能力。

推荐的数据接口：

```gdscript
var active_effects: Dictionary[StringName, Dictionary] = {}
var effect_charges: Dictionary[StringName, int] = {}

activate_item_effect(item_id: StringName) -> void
has_effect(effect_id: StringName) -> bool
get_effect_value(effect_id: StringName, default_value: float = 0.0) -> float
add_effect_charge(effect_id: StringName, amount: int = 1) -> void
consume_effect_charge(effect_id: StringName) -> bool
enter_hazard(hazard_id: StringName) -> void
leave_hazard(hazard_id: StringName) -> void
get_effect_summary() -> Array[Dictionary]
```

#### 5.11.2 疾跑与移动手感

疾跑是大地图的短时间爆发工具，不是永久提高基础速度。初始参数只作为灰盒起点，必须通过 10–20 分钟试玩调整：

```text
base_speed       = 190 px/s
sprint_speed     = 300 px/s
sprint_capacity  = 1.4 s
regen_delay      = 0.65 s
regen_time       = 1.6 s
```

- 默认按住 `Shift` 疾跑，设置中支持“长按”和“按一下切换”两种模式；手柄使用左摇杆按下或对应肩键。
- 疾跑时芽尖前倾、轨迹变长、镜头轻微拉远、播放独立移动循环；体力不足时显示喘息动画和低音提示，不突然把速度砍到零。
- 开局底部安全区必须有一段 3–5 秒的安全教学：文字只提示一次，同时使用芽体残影和地面裂纹指向，让玩家理解疾跑能跨过危险区。
- 仪式检查点、剧情锁定、长按交互和结局演出期间禁用疾跑；碰撞、相机边界和 `input_distance` 仍按正常规则统计。
- 基础疾跑不自动增加 `noise`，否则会使所有路线都受到隐藏惩罚；只有特定道具或高噪音区域可以增加 `sprint_noise`，并在状态栏说明。
- 疾跑不能替代地图内容。连续空跑时间仍不超过约 10 秒，路线时长应来自侧室、交互、地标和选择。

#### 5.11.3 三件旧神遗物

这三件遗物既是结局条件，也是最早让玩家感到“世界规则已经改变”的能力。它们的被动效果不得让普通路线直接失效。

| `item_id` | 被动/环境效果 | 反馈与地图变化 | 代价或限制 |
|---|---|---|---|
| `corrupt_eye` | 激活 `eldritch_sight`：扩大弱光轮廓和隐藏线索感知范围，靠近隐藏物时提前出现轻微音调 | 眼纹睁开一档；小地图显示附近未确认的模糊脉冲；动态提示改为“旧神正在看见某物” | 保留原有 `corruption` 计数；新增的 `attention` 只用于视觉/音频干扰，不直接锁死结局 |
| `scarlet_ointment` | 激活 `scarlet_resistance`：赤柱、辣油和高温主题区域的减速/蓄力压力降低，建议区域倍率约 `0.70`、蓄力时间约 `0.85` | 红色流光沿芽体流动；进入赤柱层时显示热浪方向；相关侧路在小地图上由模糊变为扇区级 | 在红色危险区疾跑会累积少量 `sprint_noise`；离开该区域后不继续增加 |
| `frozen_heart` | 激活 `frost_immunity`：免疫 `frost_area` 的环境减速和寒冷持续消耗，不清除已收集霜晶 | 进入霜毒区时冰晶仍有视觉反馈，但速度、输入和镜头不再被环境拖慢；状态栏显示“霜冻免疫” | `frost_crystal_1–4` 仍正常增加 `frost_count`，永冬结局的静止和压缩机条件不改变 |

旧神三件遗物的效果只在拾取完成时激活一次；重复拾取必须幂等，不能重复叠加倍率。

#### 5.11.4 遗物、养分与状态资源

| `item_id` | 机制效果 | 设计目的 |
|---|---|---|
| `golden_scale` | 激活 `sun_guidance`：玩家局部光照半径增加约 20%，已发现地标获得一次金色闪烁；不直接显示隐藏道具 | 奶龙路线的“日”概念变成探索能力，而不是只有配方材料 |
| `sleeping_stone` | 激活 `stasis_breath`：静止约 1 秒后，霜毒、黏液和环境脉冲的影响逐渐下降；重新移动后效果缓慢消退 | 支持玩家观察环境和完成永冬路线，不用一直移动躲避系统 |
| `black_water_core` | 激活 `black_blood`：黑水/黏液减速降低约 40%，并能与垃圾大陆的腐化节点进行特殊交互 | 保留原有腐化值 `3` 和不可逆感；拾取时必须明确显示“这不是普通强化” |
| `frost_crystal_1–4` | 每枚增加 `frost_count` 和一档霜化视觉；第四枚后允许压缩机节拍在小地图边缘显示一次 | 保留现有永冬目标，不额外加入会让结局变得不可理解的永久减速 |
| `nutrient_1–6` | 长按吸收后触发约 4 秒 `growth_burst`：加速芽体生长轨迹和短暂提高疾跑恢复；每次仍增加 `devour` | 让“吞噬”具有即时行为反馈，但不改变结局判定所需的吞噬数量 |
| `milk_drop_1` | 增加少量疾跑容量，芽体获得第一档乳白尾迹 | 让第一滴牛奶立即改变移动手感 |
| `milk_drop_2` | 提高加速度，转向更灵敏；芽体出现小角/高光阶段 | 让第二滴牛奶改变操控而不是只改颜色 |
| `milk_drop_3` | 提高疾跑恢复速度；获得短暂的轻盈音效层 | 让第三滴牛奶成为完整移动构筑的收束 |
| `clean_water_1–3` | 每滴提供一个 `purity_charge`；接触对应污染区或帮助同伴时消耗一层，清除一个临时负面状态 | 使“净化/分享”成为可选择的行为，而不是只满足路线计数 |

净水、牛奶、养分和霜晶的计数仍由原有 `counters` 管理；新增效果使用独立 `effect_charges`，避免背包数量和结局条件发生不同步。

#### 5.11.5 线索、组件和方向能力

| `item_id` | 机制/效果 | 触发时的提示 |
|---|---|---|
| `date_tablet` | 激活 `time_echo`：下次压缩机启动前，在 HUD 显示一次更清楚的节拍预告；小地图记录日期石片所在层 | “第三个月仍在倒数。” |
| `flowerpot_receipt` | 激活 `garden_memory`：接近可播种地点时显示花盆轮廓，不直接显示精确坐标 | “这里曾经有人给它添过土。” |
| `sale_tag_1` | 激活条码脉冲：靠近条码终端时发出一次短促提示 | 终端边缘闪烁一次 |
| `sale_tag_2` | 允许小地图显示条码路线所在扇区 | 显示模糊的橙色扫描带 |
| `sale_tag_3` | 三张价签齐全后，条码终端交互蓄力时间降低约 20%，并显示返回捷径 | “增长经文已经可以被机器读懂。” |
| `door_soil` | 激活 `warm_scent`：在中下层显示门外暖风的大致方向；靠近门缝时音量渐增 | 芽体朝暖风方向轻微偏转，不弹出直接箭头 |
| `aluminum_foil` | 激活 `conductive_sense`：靠近 ROOT 节点时出现微弱电弧，允许玩家先观察节点顺序 | 电弧只在 180 px 内出现，避免远距离剧透 |
| `conductive_water` | 激活 `bridge_charge`：ROOT 节点正确交互后短暂保持连通，错误一次不会立刻清空全部进度 | 节点之间出现一条短暂水银色线路 |
| `magnet_core` | 激活 `magnetic_pulse`：靠近金属搁架时显示隐藏线路和可用侧向捷径 | 小地图新增磁场扇区纹理，不显示所有物品 |
| `hongsan_label` | 激活 `cold_culture`：进入根网层后显示紫色菜薹根的方向级标记 | “它喜冷凉，却不能被永冬吞没。” |
| `yellow_petals` | 激活 `living_root_sense`：根网附近的暖光、钟声和可帮助对象变得更清楚 | 花瓣粒子朝仍存活的根飘动 |
| `sprout_nodule_1–3` | 每枚增加一个 `downward_seed_charge`；接近播种点时显示可种植轮廓，完成后生成向下捷径 | 背包明确显示“可种植结节：1/3” |
| `clean_nutrient` | 激活 `sterile_growth`：播种时使新芽捷径稳定，并清除播种点的临时腐败视觉 | 不允许直接吞噬，继续保留“适合播种”的路线语义 |
| `compost_label` | 激活 `cycle_notice`：垃圾大陆区域显示清理顺序和可堆肥节点的方向级提示 | “垃圾不是终点，它只是另一种土。” |

线索类物品不直接给予结局答案，只增加可读性；组件类物品应优先改变路线、节点容错或地图知识，而不是增加攻击数值。

#### 5.11.6 非背包交互物的效果

下列对象不计入 `GameState.items`，但必须通过统一事件报告对地图和状态产生影响：

| 对象 | 交互结果 |
|---|---|
| `titan_bottle` | 重撞增加 `noise` 和瓶体撞击计数；普通擦碰只播放轻声，不能让玩家因探索碰撞直接失败 |
| `yellow_cap` | 完成顺时针仪式后短暂打开黄色回环，并更新小地图的捷径状态 |
| `bottle_lever` | 解除赤柱附近的实体阻挡或开启侧向路线；只改变可达性，不自动完成仪式 |
| `companion_onion` / `companion_garlic` / `companion_ginger` | 帮助后各自提供一种短时环境音层和小范围路线提示；三者全部唤醒后触发共生和声层 |
| `root_network` / `root_network_exit` | 激活中层返回捷径；首次使用必须播放明显的根须生长演出 |
| `hongsan_root` | 只有在标签、花瓣、净水和暖光条件满足时才完成帮助；完成后把紫色根网连接到 `hongsan_link` |
| `bell_source` | 播放一次远距离钟声，作为方向线索；不能直接把玩家传送到目标 |
| `warm_light` | 作为紫冠路线的视觉终点，接近时降低 HUD 干扰、提高暖色环境音 |
| `planting_site_1–3` | 消耗一个对应芽眼结节，生成一扇向下的可穿越门；门的碰撞和小地图状态必须同时更新 |
| `downward_trial` | 要求玩家持续向下并显示方向残影；中途停下只重置试炼进度，不删除已获得道具 |
| `narrator_command_1–3` | 记录“拒绝继续生长”的叙事选择，降低后续提示强度，让玩家主动寻找自我剪芽地点 |
| `self_prune` | 完成自我剪芽后暂时禁用疾跑，播放芽尖收缩和环境静音演出；只在结局确认后恢复输入，不修改已收集资源 |
| `cleanup_zone` | 先清理垃圾袋和黑水，再开放可堆肥二段；清理顺序、噪音和破坏值必须在 HUD 中可追踪 |

#### 5.11.7 被动效果的 UI、音频和冲突处理

- 背包每个已获得道具增加“效果”一行；没有效果的线索显示“已记录到探索知识”，不要显示空白。
- HUD 同时最多显示 4 个状态图标，超过时按“当前区域影响 > 即将失效 > 常驻被动”的优先级轮换；完整列表仍可在背包查看。
- 状态图标必须有形状、颜色和文字缩写三重区分，不能只使用红/绿颜色。霜冻使用冰晶，腐败使用眼纹，疾跑使用芽尖火花，净化使用水滴。
- 获得效果时播放一次短音效，进入/离开危险区时播放状态变化音；不要每帧重复播放提示。
- 负面状态必须提供来源，例如“黏液减速：腐败黑水”“疾跑噪音：猩红圣膏”；玩家按 `I` 查看时能看到剩余时间或解除条件。
- 多个减速来源同时存在时，显示合并后的最终倍率和前两名来源；退出区域后只移除对应来源，不能清空其他状态。
- `ending_resolver.gd` 只读取物品、计数、行为值和已完成仪式；它不读取临时倍率，防止玩家因为短时被动效果意外触发或失去结局。

#### 5.11.8 实现与测试顺序

第一批只实现高收益、低风险内容：疾跑、状态来源/抗性、`frozen_heart`、`corrupt_eye`、`scarlet_ointment`、状态栏和背包效果文本。第二批加入净水、线索、组件和路线捷径。第三批再加入 `black_water_core`、多状态叠加和可选辅助设置。

自动测试至少覆盖：

1. 100 个 Seed 下疾跑不会穿过世界边界、仪式检查点或实体碰撞。
2. `frozen_heart` 只免疫环境霜冻，不影响四枚霜晶计数和结局 07 条件。
3. 三件旧神遗物重复触发时效果幂等，腐败计数和背包数量不重复增加。
4. 进入/离开多个危险区时，速度来源正确叠加和移除；重新开始后所有临时效果清空。
5. 每个现有道具至少有一个可观察反馈；线索、组件、净水和芽眼结节的地图状态都能更新。
6. 12 个结局的原有自动测试全部通过，尤其是结局 03、07、09、11、12 不被新增被动效果误触发或误锁定。
7. 1280×720 下状态栏、背包、提示、地图和对白不重叠；键盘与手柄都能完成疾跑和关闭提示。

### 5.12 `MapKnowledgeManager`：探索知识与地图标记

大地图不能把所有随机坐标直接画给玩家。新增 `map_knowledge_manager.gd`，只保存玩家在本局已经看见、走过或通过线索推断出的信息。它不拥有真实世界节点，只保存稳定 ID、世界坐标、显示精度和来源；新一局随 `run_seed` 一起清空并重新建立。

建议数据：

```gdscript
var discovered_cells: Dictionary[StringName, bool] = {}
var discovered_landmarks: Dictionary[StringName, bool] = {}
var markers: Dictionary[StringName, Dictionary] = {}

# marker 字段
# id / target_id / world_position / icon_id / color
# reveal_mode: direction | sector | approximate | exact
# source_id / priority / expires_after / clear_rule
```

地图信息分四级，任何提示规则必须明确自己授予哪一级：

1. `direction`：只在小地图边缘显示大致方向，不显示距离。
2. `sector`：显示目标位于哪个扇区或生态环，误差可以是 600–1200 px。
3. `approximate`：显示一个带范围圈的估计位置，不把图钉压在物体正上方。
4. `exact`：显示准确图标，只给已经被明确“指明位置”的线索。

标记需要有完整生命周期：获得线索时添加，目标被拾取或状态失效时立刻清除，路线改变时更新；不能留下指向已经消失物体的旧图钉。重复获得同一线索只能升级精度或刷新表现，不能生成多个重叠标记。

关键 API：

```gdscript
discover_cell(ring_index: int, sector_index: int)
discover_landmark(landmark_id: StringName, world_position: Vector2)
add_or_update_marker(marker_id: StringName, data: Dictionary)
clear_marker(marker_id: StringName)
get_visible_markers() -> Array[Dictionary]
world_to_map(world_position: Vector2, map_rect: Rect2) -> Vector2
```

小地图、完整地图、调试面板都读取这个管理器。Debug 面板可额外开启“显示真实生成布局”，但 Release 中绝不能绕过知识状态。

### 5.13 `GuidanceDirector`：事件驱动的动态提示

提示由事件触发，不做每帧扫描。拾取、仪式进度、进入扇区、重复尝试入口、长时间无有效互动等事件统一交给 `guidance_director.gd`。规则优先放在 `guidance_rules.csv`，避免把大量故事文字和坐标判断散落在关卡脚本里。

建议字段：

```text
rule_id,event_id,condition_tag,text_id,presentation,feedback_id,marker_target,marker_mode,clear_rule,cooldown,priority
```

`presentation` 至少支持：底部对白、画面中央神谕、短暂角落提示、无文字；`feedback_id` 可以触发角色停顿、转向、颜色变化、粒子、远处闪光、环境声或小地图干扰。提示排队仍由 `NarrativeManager` 负责，高优先级神谕可以延后普通拾取文字，但不能粗暴覆盖正在播放的结局演出。

提示强度采用逐级递进：

- 第一次接触只给气氛或含义，让玩家建立联想。
- 收到第二条相关线索后，可给生态环、扇区或方向。
- 明确写着“指明位置”的线索才给准确图钉。
- 同一入口连续失败两次，可把缺失提示从诗意文本升级为较清楚的方向提示。
- 90 秒没有拾取、交互、发现新扇区或完成仪式进度时，只提示最近的已知目标；未知隐藏结局不能因此自动暴露。
- 玩家可在设置中关闭自适应加强，但保留必要的操作反馈。

提示历史放入背包中的“启示”页。玩家错过中央文字后可以重读，但历史记录只保存文字和来源，不永久保存已经按规则清除的目标图钉。

十二条路线应使用不同的反馈语言，避免所有结局都变成“拾取 A 后地图标记 B”：

| 路线 | 主要提示媒介 | 地图使用方式 | 独特反馈 |
| --- | --- | --- | --- |
| 01 默认破晓 | 盒盖漏光、裂缝和上方风声 | 只显示外缘与已发现盒盖 | 越接近出口，玩家柔光颜色越暖 |
| 02 万芽之主 | 神明凝视、紫色神谕 | 猩红圣膏后准确标记永冻之心，取得后清除 | 腐败之眼让画面边缘短暂睁眼；永冻之心后只提示完成仪式，不标仪式位置 |
| 03 奶龙降生 | 童谣、乳白回声、金色闪光 | 金鳞可显示龙像所在扇区，不直接显示绕圈轨迹 | 每滴奶使芽体短暂变圆，龙像在远处回应一次 |
| 04 真正的破晓 | 暖风、尘粒和现实物品文字 | 小票只给暖风方向，杠杆完成后显示新开启的捷径 | 玩家自动朝风向轻转，门缝附近黑暗变浅 |
| 05 自治森林 | 同伴对话、根须脉冲 | 第一位同伴被浇灌后显示另外两片可能区域；连通后画出根网 | 已帮助的同伴在玩家迷路时以声音回应，而不是持续图钉 |
| 06 无限增长 | 条形码、季度数字和终端播报 | 三张价签逐步拼出终端扇区；养分不逐个标记 | 芽体轨迹被 UI 统计成增长曲线，提示语越来越企业化 |
| 07 永冬胚种 | 结霜、低频压缩机和节拍 | 显示第七格温控区域，不显示自身成环位置 | 霜晶越多，声音与动画越慢；完整周期用环境冻结反馈确认 |
| 08 ROOT 权限 | 终端字符、电弧和坐标故障 | 组件齐全后扫描探针扇区，图标呈故障闪烁 | 每件组件改变一部分 UI，正确节拍后短暂恢复清晰网格 |
| 09 紫冠的新芽 | 花瓣、钟声和暖光 | 钟声用小地图边缘波纹表示方向，不使用固定箭头 | 菜薹根主动弯向玩家，暖光让紫色逐渐转为新芽绿 |
| 10 不见天日的丰收 | 母薯牵引、地下震动和芽眼 | 只记录三处已播种位置，返回时显示通往母薯的根线 | 持续向下时相机和粒子产生反向上升感 |
| 11 今天不长 | 旁白命令、沉默和角色拒绝 | 不提供目标标记；地图逐步淡去不必要的诱导物 | 正确拒绝时芽尖收拢，错误生长时旁白语气改变但不立即失败 |
| 12 垃圾大陆之王 | 噪音投诉、碰撞声和堆积变化 | 只显示玩家制造的破坏热区，不显示最终清理点 | 地图随破坏增加杂讯和垃圾图案；可堆肥标签改变二段演出的语气 |

结局 02 的三阶段文字暂定为：

```text
腐败之眼：某位古老的神明正在注视着你。
猩红圣膏：古老的神明微微颤动，祂为你指明了某个方向。
永冻之心：古老的神明眷顾着你，请完成最后的仪式。
```

第二句播放时添加 `frozen_heart` 的准确地图标记；第三句播放前先清除该标记。即使随机生成导致玩家先取得永冻之心，也不能重新显示已经无意义的标记，应改播一条兼容顺序的神谕。

---

## 6. 十二结局的程序判定

### 6.1 不要每帧自动判定

结局只在明确的“结局请求点”判断。例如玩家顶开盒盖、进入门缝、完成根网、钻入探针、主动剪断或触发外部清理时，调用：

```gdscript
EndingResolver.request_ending(trigger_id, GameState.make_snapshot())
```

这比把十二个条件每帧互相比较更稳定，也能减少多个结局同时满足的冲突。

若某个特殊入口条件不足，只显示“还缺少什么”的模糊提示，不应自动切到默认结局。只有顶开普通盒盖时，才允许回退到结局 01。

### 6.2 统一结局 ID

```text
ending_01_food_failure
ending_02_lord_of_sprouts
ending_03_milk_dragon
ending_04_true_dawn
ending_05_autonomous_forest
ending_06_infinite_growth_inc
ending_07_eternal_winter_seed
ending_08_root_access
ending_09_purple_crown
ending_10_dark_harvest
ending_11_not_growing_today
ending_12_landfill_king
```

这些 ID 一旦进入存档就不再改名。显示名称放在 CSV 中，可以随时修改而不破坏图鉴。

### 6.3 条件表

| 结局 | 触发点 `trigger_id` | 必要条件 | 禁止或排除条件 | 条件不足时提示 |
| --- | --- | --- | --- | --- |
| 01 生长成功，食用失败 | `top_lid` | 顶开最终盒盖 | 没有其他特殊入口已经成功 | “破晓就在上方。” |
| 02 万芽之主 | `light_switch_hold` | 三遗物、逆时针瓶仪式、压缩机启动至少三次、持续按灯 7 秒 | 无 | 根据缺项提示“仍有腐星未归位”等 |
| 03 奶龙降生 | `dragon_shrine_idle` | 三滴奶、日之金鳞、绕黄色瓶盖、在小龙图案前静止 3 秒 | 未吸收腐败黑水和辣油 | “白母仍少一滴泪”或“日鳞尚缺” |
| 04 真正的破晓 | `warm_door_gap` | 两滴干净水、门缝泥土、小票背面、瓶子杠杆完成、对伸手不攻击 | 不吸收腐液/霉菌/辣油，尽量不撕膜 | “真正的出口会带来风。” |
| 05 保鲜层自治森林 | `root_network_complete` | 三名同伴均短触唤醒、分别送水、三地根系连接、返回下层 | 不能吞噬任一同伴 | “还有一位沉睡者没有回应。” |
| 06 无限增长有限公司 | `barcode_terminal` | 三张价签、所有可选养分被吸收、终点解锁后曾回头、长度/枝条达标 | `nurture == 0` | “本纪元增长目标尚未完成。” |
| 07 永冬胚种 | `freezer_alcove` | 四霜晶、眠石、温控第七格、静止一整个压缩机周期、自身成环 | 无 | “第七格不是死亡，而是等待。” |
| 08 ROOT 权限 | `temperature_probe` | 铝箔、导电冷凝水、磁芯、节点节奏正确 | 无 | 终端显示缺失组件或 `ACCESS DENIED` |
| 09 紫冠的新芽 | `hongsan_root_link` | 菜薹标签、黄花瓣、干净水、紫色菜薹根已帮助、听到钟声、向暖光、完成连接 | 温控不能在第七格；不能吞噬菜薹根 | “她喜冷凉，却不属于永冬。” |
| 10 不见天日的丰收 | `mother_soil` | 三芽眼结节、干净养分、返回母薯、三处播种完成、持续向下 | 盒盖未打开 | “还有一扇门没有被种下。” |
| 11 今天不长 | `self_prune` | 教学后返回起点、零养分吸收、零攻击、多次响应旁白命令时保持静止、长按向下剪芽 | `devour == 0`、`destruction == 0` | 旁白逐级焦躁，不直接显示配方 |
| 12 垃圾大陆之王 | `forced_cleanup` | 高噪音、高破坏、高腐化、瓶子重撞至少三次、所有保鲜膜撕裂、吸干黑水、反复撞盒盖 | `nurture == 0` | 脚步声和“噪音投诉 +1”预告；读过可堆肥标签则播放二段 |

表中“高”“所有”“多次”等词必须在调试完成后冻结为整数。第一版建议：

```text
noise >= 6
destruction >= 5
corruption >= 3
bottle_hit_count >= 3
lid_hit_count >= 3
narrator_refusal_count >= 3
milk_count == 3
frost_count == 4
barcode_count == 3
```

### 6.4 冲突处理

优先使用唯一触发点解决冲突，而不是依赖一个很长的全局优先级列表。若同一触发点仍可能匹配多个规则，使用：

```text
精确配方彩蛋
> 特殊仪式
> 指定物理出口
> 行为倾向
> 默认结局
```

每次判定在 Debug 控制台打印：触发点、满足规则、失败规则及首个缺失条件。Release 构建不显示这些调试信息。

### 6.5 结局数据与演出

`ending_text.csv` 建议字段：

```text
ending_id,index,title,category,line_1,line_2,line_3,subtitle,art_path,music_id,post_delay,next_action
```

十二结局复用同一个 `ending_screen.tscn`：

1. 锁定玩家输入。
2. 停止普通文本和危险音效。
3. 淡出或白闪。
4. 加载该结局的完整插画；插画缺失时显示程序占位，标题、文案、按钮和屏幕反馈统一由程序像素 UI 叠加。
5. 播放对应音乐和短演出。
6. 显示标题、编号和正文。
7. 写入 `ProgressState`。
8. 显示“再次发芽 / 结局图鉴 / 返回菜单”。

结局 12 如果读过可堆肥标签，在第一次结算八秒后继续播放填埋场二段；它仍然是同一个 `ending_id`，不能在图鉴中占两个格子。

---

## 7. 关卡与镜头实现

### 7.1 自底向上的 25600×25600 程序化主关卡

第 0–13 小时的 1280×3300 灰盒和第 13–20 小时的 2560×7600 纵向扩展图都属于已完成的集成原型，用于验证移动、三条结局与存档；第 20–32 小时的圆形图只保留为历史技术基线。最终主关卡恢复为自底向上的纵向探索图，逻辑边界仍固定为 `25600×25600`，出生点约为 `(12800, 23600)`，主要探索方向为向上，最终出口位于顶部区域。

当前玩家基础速度约 190 px/s，从底部出生区直线走到顶部有效区域约需 110–125 秒。地图变大后不应用一条空直线填充时长；应让玩家每 20–40 秒遇见地标、侧向路线、交互或环境变化，完整路线时间由绕行、判断与互动组成。

推荐按世界 `y` 坐标划分七个自底向上的生态层：

```text
y = 22600–24400   底部核心 / 母薯安全区 / 基础教学
y = 19300–22200   第一层：腐土与现实线索
y = 15700–18900   第二层：保鲜膜、黄瓜与早期分支
y = 12100–15300   第三层：同伴、养分与根网
y = 8500–11700    第四层：泰坦赤柱、冰霜和遗物
y = 4800–8100     第五层：温控、终端与基础设施
y = 1200–4400     顶部外壳、盒盖、门缝及特殊出口
```

### 7.1.1 垂直层间连通规则（当前设计决策）

七个层编号只用于生态主题、道具生成约束、小地图颜色和叙事节奏，不代表楼层墙体。玩家从底部向上探索，也可以在任意层向左/向右寻找侧室、回环和捷径；层间过渡必须开放，不设置横向封闭墙或必须寻找的缺口。

后续程序实现必须遵守：

1. 不生成环墙或层间横墙；`ring_gates`、`wall_rects` 只保留兼容字段或迁移数据，不能参与玩家通行判定。三条路线改为三条大体垂直的导航带，允许在层间自然偏移。
2. 层间只保留视觉过渡：颜色、材质、冷凝水密度、搁架方向、环境音和地标轮廓可以随 `y` 坐标渐变，但不能画出与碰撞不一致的封闭边界线。
3. 仍然保留道具、硬包装、辣酱赤柱、终端、盒盖、门封等真实实体的碰撞；地图只保留四条 `25600×25600` 外边界碰撞，防止玩家离开世界。
4. 生成器继续验证底部出生点到所有关键节点的可达性、区域/道具重叠、仪式净空和 100 Seed 复现；改为验证每个层至少存在多个开放上下通行带、侧向探索空间和可识别地标。
5. 小地图显示当前生态层、垂直进度、已发现侧室和上下方向提示；玩家跨越层边界时只揭开新的知识区域，不触发卡墙或强制绕行。

本改版由圆形地图切回垂直地图，程序实现与本节同步完成；旧版运行代码中的环墙碰撞已经改为世界外边界碰撞，后续不得恢复环墙。

垂直地图不是一条单线走廊。最终版每局生成 12–16 个横向扇区，三条大体独立的上下路线，层内侧室、横向回环、短路和返回下层的捷径；所有结局仍复用同一张逻辑地图，不制作十二张地图。

先生成抽象可达图，再生成视觉和实体物件：

1. 创建七个自底向上的生态层与 12–16 个横向扇区格。
2. 为每层选择上下主路线、横向侧室、危险区、开放过渡带和地标槽位。
3. 确保相邻生态层在多个扇区存在足够宽的开放上下通行带，不生成连续横墙或“必须找门”的层间瓶颈。
4. 用图搜索验证底部出生点、关键物品、仪式和所有出口的可达性。
5. 再把图节点转换成区域、装饰、硬质物件和导航提示；环带本身不转换成碰撞墙。
6. 最后放置道具与动态提示目标，并建立小地图知识初始状态。

每个扇区至少有一个可识别主题，例如破裂抽屉、霜柜、酱料墓地、根网温床、条形码仓库、失电控制区或垃圾堆场。主题决定配色、环境音和可能生成的物品类型，但不强制绑定唯一结局，防止玩家一看扇区就知道答案。

地图、区域和道具使用受约束的种子化生成，不做完全无规则随机：

- 每局生成并在 `GameState`、Debug 面板中记录 `run_seed`；相同种子必须复现相同房间连接、区域与道具位置，方便复现 Bug。
- `procedural_map_generator.gd` 先生成可行走图、扇区区域、开放过渡带和捷径，再做连通性验证，最后才放置黏液、冰霜、仪式、出口、线索和拾取物。
- 每类区域和物品声明允许出现的生态层、扇区数量、与同类/出生点/主路线的最小距离。随机性改变扇区连接方式和具体摆放，不改变 `item_id`、结局条件或配方。
- 关键配方必须满足“需要它的仪式或出口之前可到达”；任何拾取物不得生成在实体碰撞体、危险区、其他拾取物、仪式检查点或出生安全区内。
- 仪式与出口周围预留固定净空，检查点中心到最近碰撞边缘至少为“玩家碰撞半径 + 32 px”，避免再次出现赤柱遮挡仪式点的问题。
- 生成后对玩家出生点执行洪泛/图搜索：所有必需节点可达、至少两个外缘出口可达、每个关键物品到对应入口存在合法路径。失败时换种子重试，最多 20 次；仍失败则使用经过验证的固定后备布局。
- 连续无互动的直线移动不超过约 10 秒；每 20–40 秒出现地标，每 45–90 秒至少出现交互、线索、路线选择或材质变化。增加时长来自选择和发现，不来自空白面积。
- 地标的类别、色彩与生态层含义保持稳定，即使位置随机也能被学习；HUD 提供简洁的垂直进度/方向提示，不直接显示隐藏道具。
- 同一结局的关键节点不得全部生成在同一直线或同一扇区；至少跨越两个生态层或两个相邻扇区，形成真实探索链。
- 不同结局的关键物品允许空间交错，鼓励玩家临时改变路线，但互斥条件必须在交互前给出可理解反馈。
- 顶部外壳不能只是连续视觉墙壁。每局至少生成两个普通出口候选、三个特殊入口区域和两条可从顶部快速返回中下层的捷径；外壳结构只在必要的实体模型处产生碰撞。
- 底部安全区、每个生态层的代表地标和顶部外壳保持稳定语义；随机性改变位置和连接，不改变玩家可学习的世界规则。

建立固定种子回归集，至少批量检查 100 个种子的连通性、关键物品顺序、碰撞重叠、仪式净空和生成耗时。最终游玩时长使用 `GameState.run_seconds` 和 `input_distance` 实测：第一次不知道路线的玩家到默认结局目标为 10–20 分钟；熟悉布局规则后直达默认结局约 5–8 分钟；主动寻找隐藏结局可以超过 20 分钟。若盲测时间不足，优先增加路线选择、短事件、可理解的绕路与支线交互；若超时主要来自迷路，优先增加地标、知识标记和捷径，不直接提高移动速度掩盖问题。

### 7.2 大地图分块与性能

25600×25600 本身不会造成严重性能问题，真正的风险是同时存在太多节点、碰撞体、灯光和持续处理脚本。生成器以 1280×1280 或相近尺寸划分逻辑块：

- 玩家所在块及周围两圈块保持激活；更远装饰物只保留轻量数据或合并绘制。
- 关键拾取物、仪式和入口的状态始终由 ID 保存；卸载视觉节点不能丢失已拾取或已完成状态。
- 层带背景和重复装饰优先使用批量绘制、TileMap/MultiMesh 或合并几何；不为层间边界生成数百个独立碰撞体或 `_process()` 节点。
- 远处灯光、粒子和动画默认停用；环境声按生态层和主题扇区混合，不给每个物体单独挂持续音频播放器。
- 小地图从生成计划和知识状态绘制，不使用第二台相机实时渲染整个 25600×25600 世界。
- 该坐标规模不需要浮点原点重置。若未来继续扩展到数十万像素，再单独评估浮点精度方案。
- Debug 面板显示活动块数、节点数、生成耗时和当前 FPS；活动块切换不能造成明显停顿。

生成目标：普通电脑首次生成不超过 1 秒，换 Seed 重开不超过 2 秒；稳定游玩时不因跨块产生超过 100 ms 的单帧卡顿。比赛最终版如果达不到该目标，先减少装饰密度和动态灯光，不删除结局所需节点。

### 7.3 小地图与完整地图

小地图是“玩家当前理解的世界”，不是开发者俯视图。`minimap_panel.tscn` 放在右上角，默认约 220×220 px 的方形地图窗；按 `M` 打开 `world_map_panel.tscn` 查看完整的自底向上地图，再按 `M` 或 `Esc` 关闭。背包和完整地图不能同时打开，后打开者应先关闭前一个，暂停菜单优先级最低。

小地图显示层从下到上为：

```text
已探索扇区底色
生态层分区渐变与已发现通路
稳定地标
玩家轨迹 / 当前朝向
剧情知识标记
临时方向波纹与危险提示
```

- 固定上方为地图前进方向，不随玩家旋转，降低迷失感；玩家箭头显示朝向。
- 底部母薯使用永久图标，玩家随时可以辨认回程方向。
- 进入新生态层/扇区后揭开对应格，并可额外揭开相邻格的一小部分边缘，避免黑雾边界过于机械。
- 大型实体结构只画主要轮廓，不逐像素复制场景碰撞；层间只显示渐变和材质变化，捷径或地标状态改变后才显示相应导航反馈。
- 隐藏道具默认不显示。剧情标记根据 `direction/sector/approximate/exact` 精度绘制不同形状。
- 标记使用图形、颜色和文字缩写三重区分，不能只依赖红绿颜色；提供小地图缩放和关闭动态闪烁选项。
- 1280×720 下不得遮挡背包、对白、交互提示和结局画面；中央神谕出现时，小地图可降低亮度但不能丢失状态。
- 全屏地图支持查看图例、当前 Seed、生态层名称、已发现地标和“启示”摘要；不显示未解锁结局名称。

坐标换算统一使用 `25600×25600` 世界矩形，不把圆形半径写死在 UI：

```gdscript
var normalized := world_position / Vector2(25600.0, 25600.0)
var map_position := map_rect.position + normalized * map_rect.size
```

地图边缘标记需要裁剪到矩形范围；`direction` 模式只显示指向上方/侧方的箭头，不能通过边缘图标大小泄露实际距离。

### 7.4 地标、捷径与防迷路结构

小地图不能独自承担导航。每个生态层至少放置一个远距离可识别的大地标，并使相邻主题拥有不同颜色、轮廓和环境音。玩家向上推进时，地标尺寸和亮度逐层变化，保证在局部黑暗中仍能通过轮廓或声音判断所在高度。

- 上下主路使用贯穿多层的裂纹、冷凝水或根须纹理。
- 横向回环使用货架边、塑料膜接缝或冰霜带，玩家可从地面语言判断向左/向右的探索方向。
- 每完成一条路线的中间目标，至少打开一条回程捷径，减少重复走过已经理解的区域。
- 死路必须含有奖励、故事、视野信息或快速返回方式，不能只有空墙。
- 玩家连续两次回到同一扇区但没有新发现时，可以让当前地标发出更明显的声音，而不是直接弹出箭头。
- 生态层过渡处使用色温、环境音和地面材质同时变化，让不看 HUD 的玩家也能意识到自己正在向上或向下移动。

### 7.5 Camera2D

- 相机跟随芽尖，Position Smoothing 开启。
- 四向范围限制在本局 `25600×25600` 矩形地图内；四条外边界使用实体与遮罩阻止玩家进入无内容区域。
- HUD 显示玩家垂直进度、所处生态层和小地图；完整地图仅在按 `M` 后显示。
- 普通碰撞轻微震动，只有重撞和终局使用明显震动。
- 结局演出时相机从玩家身上解绑，由 AnimationPlayer 控制。
- 提供“降低屏幕震动”选项，默认 100%，允许调整到 0%。

### 7.6 黑暗与局部光照

首选低风险方案：深色背景 + 玩家圆形柔光 Sprite/PointLight2D + CanvasModulate。不要在 48 小时项目里依赖复杂全屏 Shader。

- 玩家附近半径约 180–260 px 可见。
- 重要道具自身有非常弱的轮廓光，但不能直接暴露完整位置。
- UI 位于独立 CanvasLayer，不受黑暗影响。
- 终局用白色 ColorRect Tween 爆白，随后关闭 CanvasModulate。

---

## 8. Debug 工具与自动测试

### 8.1 Debug 面板

F3 打开，仅 Debug 构建可用。显示：

```text
devour / nurture / noise / destruction / corruption
milk / frost / barcode / relic mask
compressor count / bottle hits / lid hits
items / flags / rituals
run seed / 当前生态环 / 当前扇区
当前附近的 ending trigger
上一次判定失败原因
```

按钮：

- 传送到四个关卡阶段。
- 添加指定道具。
- 设置行为值。
- 完成指定仪式。
- 强制请求十二个结局入口。
- 重置本局。
- 清空永久图鉴。
- 复制当前种子并用指定种子重开。
- 重新随机生成地图，并显示生成/连通性验证结果。

没有这个面板，十二结局会导致大量重复跑图，严重浪费比赛时间。

### 8.2 自动测试

不依赖第三方测试插件，使用可被 `--headless --script` 调用的 GDScript 测试脚本即可。至少覆盖：

- 十二条正向条件各能返回正确 `ending_id`。
- 每条特殊结局分别缺一个关键条件时不能触发。
- 奶龙碰过腐败黑水后不能触发。
- 共生路线吞噬一个同伴后不能触发。
- 洪山路线温控拨到七后不能触发。
- 结局 12 是否播放二段只受可堆肥标签控制，图鉴 ID 不变化。
- 顶开普通盒盖始终有默认结局，不会卡死。
- 重新开局会清空 GameState，但不会清空已解锁结局。
- 损坏或空存档不会让游戏崩溃。
- 固定 100 个 Seed 均生成 25600×25600 边界、七个自底向上生态层和 12–16 个扇区，不使用未验证的后备布局。
- 每个 Seed 至少有三条底部到顶部的独立上下路线，所有关键物品、仪式、捷径和入口可达。
- 同一 Seed 的世界布局、生态层主题和初始知识状态完全复现；不同 Seed 必须有足够的连接与位置差异。
- 未探索区域、隐藏道具和未获得线索的入口不会出现在 Release 小地图数据中。
- `direction/sector/approximate/exact` 四种标记精度换算正确，世界边缘标记不会画出地图范围。
- 腐败之眼不显示永冻之心位置；猩红圣膏显示准确标记；取得永冻之心立即清除标记。
- 先取得永冻之心再取得猩红圣膏时，不会重新添加过期标记，并播放兼容顺序的提示。
- 重新开局会清空探索格、提示历史和临时标记，但同一指定 Seed 仍复现真实布局。
- 背包、完整地图、暂停菜单和结局画面的输入优先级正确，不会同时叠开或锁死输入。
- 分块卸载再加载后，已拾取道具不会复活，已完成仪式和已开启捷径不会回退。

### 8.3 手工测试表

每个结局至少执行：

1. 一次完整正向触发。
2. 一次缺少最后条件的失败测试。
3. 一次与相似路线的冲突测试。
4. 一次结局后重新开始。
5. 一次关闭游戏再打开，检查图鉴仍在。

大地图额外执行：

1. 至少五个不同 Seed 的无攻略盲测，记录首次到默认结局和首次发现隐藏路线的时间。
2. 故意在顶部区域迷路，确认只靠地标、地面语言和小地图能返回底部出生区。
3. 按腐败之眼 → 猩红圣膏 → 永冻之心顺序测试文字、地图标记添加和清除。
4. 打乱三件遗物的取得顺序，确认提示仍然合理，不出现已经失效的目标。
5. 反复跨越分块边界，观察 FPS、卡顿、节点复活、碰撞缺失和环境声跳变。
6. 在 1280×720 与 1920×1080 下同时测试小地图、背包、对白、暂停和结局画面。

测试人员记录：Godot 版本、构建编号、操作步骤、预期结果、实际结果、截图或录屏、是否阻塞提交。

Bug 优先级：

```text
P0：崩溃、无法通关、导出不能运行、存档损坏、结局错误
P1：明显卡住、提示缺失、音频持续播放、UI 无法操作
P2：轻微穿模、动画不顺、错别字、装饰问题
```

截止前只允许 P0 阻塞发布；P1 尽量修，P2 记录到已知问题。

---

## 9. 48 小时具体执行顺序

十二个结局都保留在设计和数据中，但程序必须按批次完成。每个阶段结束都要产生一个可玩的版本。

### 第 0–2 小时：冻结规则与建仓

- [ ] 全队确认 Godot 精确版本、1280×720、Windows、键位和文件命名。
- [ ] 确认 `ending.md` 是结局唯一规范；旧三结局逻辑不实现。
- [ ] 建立远程 Git 仓库，添加队友，完成第一次推送。
- [ ] 建立 `source/release/press/other` 目录和 `.gitignore`。
- [ ] 创建 Godot 工程、Input Map、Autoload、Audio Bus 和主场景。
- [ ] 将十二个结局 ID 写入 `ending_ids.gd`，从此不改 ID。

验收：空工程能运行到主菜单，无报错，另一名队友能拉取并打开。

### 第 2–7 小时：最小可玩竖切

- [ ] 完成芽尖四向移动、加减速和碰撞。
- [ ] 完成 Line2D 身体轨迹、相机和局部光照。
- [ ] 使用矩形/圆形占位图搭建一条从底到顶的灰盒关卡。
- [ ] 完成终局盒盖蓄力、白闪和默认结局 01。
- [ ] 完成 R 重新开始和 Esc 暂停。

验收：没有正式素材也能在 2–3 分钟内从出生点到结局 01，再按 R 重开。

### 第 7–13 小时：基础系统

- [ ] 完成黏液、保鲜膜、硬障碍、冰霜四种反馈。
- [ ] 完成通用短按/长按交互与道具拾取。
- [ ] 完成 GameState、信号和 Debug 面板。
- [ ] 完成文本 CSV、文本队列和四阶段叙事触发。
- [ ] 完成 AudioManager、压缩机事件和基础音效。

验收：不同材质反馈清楚；道具不会重复计数；文本不会互相覆盖。

### 第 13–20 小时：首批三结局

- [x] 先把主关卡坐标框架扩展到至少 2560×7200，作为首批三结局的纵向集成原型，并完成各幕主路线、侧室、回环和 `Marker2D` 传送点；第 0–13 小时小地图保留在 Git 历史中，不另建第二套正式关卡。
- [x] 将黏液、冰霜、硬障碍、保鲜膜、拾取物和叙事触发按探索节奏重新分布，禁止用大片空走区域填充时长。
- [x] 结局 01 默认土豆。
- [x] 结局 02 三遗物、逆时针仪式、第三次压缩机、灯开关长按。
- [x] 结局 03 三滴奶、金鳞、黄色瓶盖仪式、小龙图案静止。
- [x] 完成通用 EndingScreen 和结局解锁存档。
- [x] 为三结局各做一次正向、缺条件和冲突测试。

验收：前三结局从同一张纵向集成关卡稳定触发，重开后图鉴显示 3/12；本阶段重点验证系统与配方，最终 10–20 分钟游玩时长由后续 25600×25600 自底向上程序化地图实现。

### 第 20–32 小时：第二批五结局

- [x] 建立 `procedural_map_generator.gd`，把现有关卡迁移为至少 8192×8192、中心出生、向外扩展的生态环与 8–12 个扇区（历史技术基线）。
- [x] 用 `run_seed` 随机生成房间连接、区域和道具；实现 20 次重试、固定后备布局、连通性及关键道具先后顺序验证。
- [x] 完成物品栏/背包，可按 I 或 Tab 查看本局道具、分类、数量、神话名与已解锁的现实名称。
- [x] 04 真正的破晓：水、泥、小票、杠杆、暖门缝。
- [x] 05 自治森林：三同伴、送水、根网。
- [x] 06 无限增长：养分统计、回头标记、条形码终端。
- [x] 07 永冬胚种：霜晶、眠石、温控、完整压缩机周期。
- [x] 08 ROOT 权限：三材料、节点节拍、温控探针。

验收：结局 04–08 可通过 Debug 面板快速验证，至少各完整手玩一次；固定 100 个种子的地图连通性、关键物品可达性、仪式净空与碰撞重叠测试全部通过。

当前状态：自动化验收已通过（包括 100 个固定种子和 04–08 实际入口到结算/图鉴的端到端流程）；五条路线的完整人工试玩由本地体验测试继续确认。

### 第 32–39 小时：第三批四结局

- [x] 将最终地图扩展到 25600×25600、七个自底向上生态层和 12–16 个扇区；先升级抽象连通图、分块与验证，再迁移现有内容。
- [x] 建立 `MapKnowledgeManager`、右上角小地图、M 键完整地图、战争迷雾、地标和四级标记精度。
- [x] 建立 `GuidanceDirector` 与 `guidance_rules.csv`，完成提示优先级、历史“启示”页、自适应加强和过期标记清除。
- [x] 完整实现腐败之眼 → 猩红圣膏 → 永冻之心的中央神谕、精确标记与取得后清除流程，并覆盖乱序拾取。
- [x] 为结局 01–08 按各自风格接入至少一种文字之外的角色、世界、声音或地图反馈。
- [x] 09 紫冠的新芽：菜薹标签、花瓣、送水、钟声、暖光连接。
- [x] 10 不见天日的丰收：三结节、返回母薯、三处播种。
- [x] 11 今天不长：零吸收/零攻击、拒绝计数、主动剪芽。
- [x] 12 垃圾大陆之王：噪音/破坏/腐化阈值、提前清理、可堆肥二段。

验收：十二条自动条件测试通过；图鉴为 12 格；不存在旧附加结局 ID；100 个 Seed 的大地图、知识隐藏、标记生命周期和分块状态测试通过；玩家可利用地标和小地图从顶部返回底部出生区。

当前状态：程序与自动化验收已完成；地图已切换为 25600×25600 自底向上垂直结构，100 个 Seed、十二结局条件、结局 09–12 实际入口到结算/图鉴、动态标记乱序、分块和旧阶段回归需重新完成本地回归。五个不同 Seed 的无攻略人工盲测、实际 10–20 分钟时长与地标辨识仍需本地试玩确认。

本阶段回归修复记录：

- 环形地图的每层缺口曾分别加入角度抖动，导致玩家穿过一层后无法对准下一层缺口；本次切回垂直地图后，该缺口逻辑改为三条自底向上的导航带，不再生成环墙。
- 结局 11“自我剪芽”的持续向下区域经试玩二次校准：实际入口为 `480×320`、持续 `0.8 秒`，前置向下试炼为 `520×340`、持续 `0.9 秒`；以 190 px/s 的标准移速计算，玩家持续向下时有足够完成距离，同时不会覆盖过大的地图范围。
- 过渡版曾让环墙显示直接复用分段碰撞数据：内环墙体可见厚度与 `52 px` 碰撞一致，外壳与 `88 px` 碰撞一致；该方案仅保留为历史记录，不再用于最终地图。
- 本次切回垂直地图后，六道层间过渡改为三条开放式上行导航带，已移除环间边界和对应环墙绘制；`ring_gates`、`wall_rects` 仅保留兼容数据，不参与正式玩法判定。

### 第 39–44 小时：素材接入与手感

- [x] 用 `program/image` 扁平目录中的正式 PNG，以及 `program/audio/ambience`、`music`、`sfx` 中的 WAV/OGG 替换可替换的占位资源；缺失结局插画保留程序占位卡。
- [x] 通过 `AssetLibrary` 统一缓存角色、道具、交互物、区域、护栏、仪式标记、六张垂直背板和结局插画；UI、粒子及所有覆盖层统一由程序像素绘制，不读取 `ui_`、`vfx_` 图片。
- [x] 通过 `AudioManager` 自动扫描三类音频目录，按文件名注册音乐与音效，并为旧的未补零编号音效（如 `ritual_step_1`）兼容 `_01` 文件名。
- [x] 修正碰撞形状，不直接照图片透明边缘自动生成复杂多边形；美术只负责可见表现，玩法碰撞仍由显式的矩形、圆形或多边形节点控制。
- [x] 保留并统一速度、蓄力时长、暗度、音量和文本停留时间的程序参数；本阶段接入素材后完成音频与 UI 层级的默认平衡。
- [x] 窗口布局使用锚点和拉伸策略适配 1280×720、1920×1080 及较小窗口；正式验收仍需在本机实际调整窗口观察可读性。
- [x] F3 提供关键图片总览与全部 85 个当前 Seed 素材/玩法点传送；可逐层检查拾取物、交互物、仪式、危险区域与结局入口。
- [x] 玩家移动粒子按七个垂直生态层分别配色，疾跑改变数量与拖尾距离；状态效果与层级颜色混合而不依赖粒子图片。
- [x] 完成 38 个唯一拾取物与交互节点审计：消除 E 键短按/长按死区，修复调试提前获得道具后残留不可拾取世界副本，以及“帮助/吞噬”错误共用前置条件的问题。
- [ ] 让至少一名没看过答案的人试玩，记录他在哪里迷路。
- [ ] 使用不同随机种子盲测，确认变化足以鼓励探索，但地标、生态环含义与配方线索仍能被理解。
- [ ] 记录盲测玩家到默认结局的 `run_seconds` 和 `input_distance`；目标为 10–20 分钟，低于 10 分钟则补充有效探索，高于 20 分钟且主要时间花在迷路则增加地标或捷径。

> 自动检查已完成素材路径、运行时回退、音频注册、分辨率布局和场景回归；人类试玩、随机种子盲测与 10–20 分钟时长指标仍需在本机完成后再勾选。

验收：首次玩家能在 10–20 分钟内触发默认结局，并能看懂至少两条隐藏路线的提示；地图时间主要来自探索和交互，而不是空走或重复受阻。

### 第 44–47 小时：导出与回归测试

- [x] 冻结新功能，只修 P0/P1 Bug。
- [x] 创建 Windows Desktop x86_64 Release 导出预设。
- [x] 导出到 `release/`，填写 `release/README.txt`。
- [x] 在无 Godot 的电脑上完整运行一次。
- [x] 完成至少一张大于 1024×768 的截图放入 `press/`。
- [x] 检查 `source/` 包含完整工程和编译说明。
- [x] 检查 `other/` 包含团队介绍、许可证和第三方授权。

验收：从最终目录重新压缩、解压、运行，中文、音频、存档和退出正常。

### 第 47–48 小时：提交缓冲

- [ ] 生成最终 ZIP，不再改高风险代码。
- [ ] 核对 ZIP 内不是多套一层无意义目录，根目录直接看到四个规定文件夹。
- [ ] 上传完整 ZIP；GitHub 链接只能补充，不能替代 ZIP。
- [ ] 队长检查项目页面成员、简介、平台、工具和截图。
- [ ] 保留本地 ZIP、远程仓库和另一处备份。

最后一小时禁止临时升级 Godot、重写玩家控制或加入新结局。

---

## 10. 时间不够时的降级方案

“十二结局写入设计”不等于必须为每个结局做完全不同的地图、动画和音乐。按以下顺序降级：

1. 保留十二个判定和图鉴格子。
2. 每个结局至少有独立标题、文字、颜色和一张完整插画；缺失插画使用程序占位。
3. 结局 01、02、03 保留完整演出。
4. 结局 04–08 若完整插画未到位，使用程序几何占位、滤镜、音乐和文字，不恢复覆盖层拼装方案。
5. 结局 09–12 可以使用静帧、程序生成文字和现有芽体复制。
6. 程序像素粒子可降低数量和刷新频率，但不删除结局条件反馈。
7. 最后仍无法稳定时，用项目内 `feature_flags.gd` 暂时隐藏未完成入口，但不要留下可进入后卡死的半成品。

任何时候都优先保证：能启动、能移动、默认结局可完成、能重开、能导出。

---

## 11. 美术和音频接入合同

### 11.1 美术

- 运行时统一 `.png`，透明物体必须有 Alpha。
- 当前阶段只交付最终运行时 `.png`，不要求制作 `.psd` 或 `.kra`；Godot 只引用导出的 PNG。
- 同一个物体的状态使用统一尺寸和锚点，例如 `film_intact.png`、`film_stretched.png`、`film_broken.png` 画布完全一致。
- 光源方向、像素密度和描边粗细在美术规范中固定。
- 程序碰撞形状单独制作，不依赖贴图透明度。
- 不直接使用真实品牌 Logo；老干妈等只作为内部描述，正式图使用原创辣酱标签。

### 11.2 音频

- 短音效：48 kHz `.wav`。
- BGM 和环境循环：48 kHz `.ogg`，首尾无接缝。
- 文件名只使用小写英文、数字、下划线。
- 每个文件注明是否循环、推荐音量、单声道/立体声和触发位置。
- 程序保留总线音量，不通过直接修改源文件来反复调大小。

### 11.3 文案

- 文案人员修改 `.md` 源稿，程序负责人统一同步到 CSV。
- 每条文本有唯一 `text_id`，修改文字不能改 ID。
- 单次屏幕最多 1–2 行，避免玩家移动时阅读长段落。
- 结局名与 `ending.md` 完全一致。

---

## 12. Git 协作步骤

### 12.1 分支规则

小团队 48 小时内使用简单规则：

```text
main                 始终保持可运行
feature/player       玩家移动和轨迹
feature/endings      结局系统
feature/ui           UI 和文案
asset/art-batch-01   美术批次
asset/audio-batch-01 音频批次
```

每个功能完成并在本机运行后再合并到 `main`。避免多人同时编辑同一个 `.tscn`；场景冲突比脚本冲突更难处理。

### 12.2 每次工作循环

```powershell
git pull --rebase
git switch -c feature/功能名
# 修改并测试
git status
git add 明确的文件路径
git commit -m "feat: 完成某功能"
git push -u origin feature/功能名
```

不要使用 `git add .` 无脑提交缓存、导出包和私人临时文件。Agent 在提交前应展示 `git status` 和变更摘要。

### 12.3 场景冲突预防

- 一个人负责 `fridge_level.tscn` 的摆放。
- 一个人负责 `ending_screen.tscn`。
- 其他人交付素材，不直接打开并保存这些场景。
- 通用对象各自独立成 `.tscn`，主关卡只实例化它们。
- 合并前关闭 Godot，确保场景已保存且 `.godot/` 未被跟踪。

---

## 13. 导出与最终提交

### 13.1 编辑器导出

1. 打开 `Project > Export`。
2. 添加 `Windows Desktop` 预设。
3. Architecture 选择 `x86_64`。
4. Export Mode 选择全部项目资源，排除测试截图和未使用大源文件。
5. 导出路径设为 `../release/dawn_of_the_sprout.exe`。
6. 关闭“Export With Debug”。
7. 导出后检查 `.exe` 和 `.pck` 均存在。

若 Godot 已加入 PATH，Agent 可以执行等价命令：

```powershell
godot --headless --path source --editor --quit
godot --headless --path source --export-release "Windows Desktop" ../release/dawn_of_the_sprout.exe
```

第一条用于检查工程能否正常导入，第二条用于生成 Release。实际命令可能需要使用 Godot exe 的完整路径。

### 13.2 最终目录验收

```text
最终ZIP/
├─ source/
│  ├─ project.godot
│  ├─ 完整脚本、场景、素材
│  └─ README.md              # 如何用 Godot 4.7.2 打开和导出
├─ release/
│  ├─ dawn_of_the_sprout.exe
│  ├─ dawn_of_the_sprout.pck
│  └─ README.txt             # 解压、启动、按键、退出、已知问题
├─ press/
│  └─ screenshot_01.png      # 至少一张，大于 1024×768
└─ other/
   ├─ TEAM.md
   ├─ LICENSE.txt
   ├─ THIRD_PARTY_LICENSES.md
   └─ AI_USAGE.md
```

完整 ZIP 必须直接上传到比赛项目页面。GitHub 仓库、Release、视频或网页版只能作为补充链接。

---

## 14. 完成定义

程序完成不是“代码写完”，而是同时满足：

- [ ] Godot 编辑器打开工程无红色错误。
- [ ] 新玩家能完成移动教学，并在没有路线攻略的情况下通过约 10–20 分钟有效探索触发默认结局；熟悉路线后的直达时间约为 5–8 分钟。
- [ ] 十二个正式结局均有固定 ID、入口、条件、缺失提示、文本和图鉴位置。
- [ ] 十二条正向判定测试和关键冲突测试通过。
- [ ] 物品栏能准确显示本局已取得道具、分类和数量，重开后不会残留上一局库存。
- [ ] `ItemEffectDirector` 统一管理疾跑、环境抗性、地图知识和临时状态；现有道具都有可观察的效果或知识反馈。
- [ ] 永冻之心、腐败之眼、猩红圣膏的效果不破坏原有结局条件；状态来源、叠加、清除和重开测试通过。
- [ ] 最终地图为至少 25000×25000 的自底向上矩形结构；固定 100 个种子的上下连通性、关键物品顺序、碰撞重叠和仪式净空测试通过，失败生成能安全回退到固定布局。
- [ ] 小地图只显示已探索区域和已经获得的知识；M 键完整地图、地标、战争迷雾、四级标记和清除规则工作正常。
- [ ] 腐败之眼、猩红圣膏、永冻之心的三阶段提示与动态标记完整工作，乱序拾取不会产生错误提示。
- [ ] 十二条路线拥有可区分的文字、角色、世界、声音或地图反馈，不依赖同一套精确图钉引导。
- [ ] 分块加载不会复活道具、回退仪式或造成明显卡顿，25600×25600 地图在目标电脑稳定运行。
- [ ] 本局重开后行为状态清空，永久图鉴保留。
- [ ] 文本不会重叠，结局演出期间输入被锁定。
- [ ] 音乐、环境声和音效可分别调节。
- [ ] 不依赖编辑器专有状态，Windows Release 在另一台电脑可运行。
- [ ] `source/release/press/other` 齐全，许可证和编译说明完整。
- [ ] 最终 ZIP 已实际解压验证，不只是确认文件存在。

---

## 15. 第 20–32 小时版本完成后的实施顺序

当前 8192×8192 版本作为可回退基线保留在 Git 提交 `4f78f86`。下一阶段不要同时重写生成器、UI、十二结局和素材表现，应按下面顺序逐层保持可运行：

1. 只升级生成数据结构：25600×25600、七个自底向上生态层、12–16 扇区、抽象连通图与分块索引，暂时复用现有占位视觉。
2. 扩充 100 Seed 测试，确认三条独立上下路线、关键节点顺序、仪式净空、区域间距和生成耗时。
3. 实现 `MapKnowledgeManager` 与调试用俯视图，先验证世界坐标到地图坐标的换算和知识隐藏。
4. 接入右上角小地图、M 键完整地图、战争迷雾、地标、捷径和四级标记。
5. 实现 `ItemEffectDirector`，先完成疾跑、状态来源/抗性、腐败之眼、猩红圣膏、永冻之心和背包效果摘要。
6. 实现 `GuidanceDirector`，把道具被动、环境反馈和三阶段神谕流程接入统一事件队列，并覆盖乱序兼容。
7. 为结局 01–08 分别补充至少一种独特的非文字反馈，再接入结局 09–12，避免所有路线共用同一种导航模板。
8. 最后做五个以上随机 Seed 的盲测，依据实际迷路位置调整地标、通路、疾跑参数和提示强度；不要仅凭地图面积判断游玩时长。

下一阶段可以直接这样下达任务：

> 请按照 programming.md 的第 32–39 小时阶段继续制作。先把生成器升级到 25600×25600、七个自底向上生态层和分块结构，并通过 100 Seed 测试；再实现 `ItemEffectDirector` 的疾跑、三遗物被动与状态栏，接入矩形小地图、完整地图和动态提示，最后完成三遗物提示链并接入结局 09–12。需要改变结局规则或提示强度时先向我确认。

阶段中每完成上面一个层级都应保持工程可运行并单独测试。若大地图升级出现性能问题，优先保留连通图、知识系统和导航体验，降低装饰密度；不要退回单向长走廊，也不要用永久显示所有目标的方式解决迷路。
## 44 小时后：主界面、教程与可读性收尾

本轮继续保持 25600×25600 自底向上地图、七层生态和既有结局规则，仅改善表现与新玩家进入流程：

- [x] 层间 400px 过渡缝改为实体可行走过渡带，使用上下层混合色和背板纹理填充，不再露出黑色底图；过渡线只保留低对比度导向线。
- [x] 移动粒子改为更醒目的阶梯像素方块和高亮核心；静止/移动/疾跑仍分别使用七层生态色，疾跑提高数量、距离、尺寸和不透明度。
- [x] 拾取道具时弹出图文详情卡：左侧为对应道具图片，右侧为神话名称、现实名称、描述和已激活效果；自动停留数秒且不阻塞移动。
- [x] 背包条目改为可点击按钮，显示道具缩略图和摘要；点击后打开同一套详情卡，Esc 或关闭按钮返回背包。
- [x] 新增六步新手教程，覆盖移动、疾跑、E 交互、Space 蓄力、背包/地图和探索目标；首次进入游戏自动显示，也可从主菜单重复打开，支持跳过。
- [x] 新增主界面，接入 `ui_logo.png` 和 `ui_main_menu_background.png`，提供开始游戏、新手教程和退出游戏按钮；测试场景仍自动进入关卡，不影响自动化回归。
- [x] 补齐并接入 `env_rotten_cucumber_intact.png`，作为第一处腐败黑水区域的环境主体；`need.md` 状态更新为 `[C1]`。
- [x] 接入 `NotoSerifSC-VF.ttf` 全局 Theme，中文正文与标题改用史诗感衬线字形，原有像素描边和冷紫色 UI 层级保留。

验收重点：首次启动出现主菜单；开始游戏后首次教程可跳过；靠近任意道具出现图文卡；I/Tab 打开背包后点击道具可查看详情；M、F3、暂停和结局流程不受影响；七个层间过渡处不再显示纯黑横缝。对应自动检查为 `front_end_test.tscn`、`interaction_audit_test.tscn` 和 `phase_32_39_test.tscn`。
